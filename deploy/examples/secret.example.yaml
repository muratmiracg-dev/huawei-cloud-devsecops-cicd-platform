# Example only. Do not commit a populated Secret.
# Prefer Huawei Cloud DEW/CSMS or an approved secret injection mechanism.
apiVersion: v1
kind: Secret
metadata:
  name: commerce-platform-secrets
type: Opaque
stringData:
  catalog-database-url: postgresql+psycopg://USER:PASSWORD@HOST:5432/catalog
  orders-database-url: postgresql+psycopg://USER:PASSWORD@HOST:5432/orders

